name: PR Check

# Тонкая обёртка: запускает typecheck при открытии (или обновлении) Pull Request
# в ветку week4. Реальная логика лежит в typecheck.yaml — здесь только триггер.

on:
  pull_request:
    branches: [week4]

jobs:
  checks:
    uses: ./.github/workflows/typecheck.yaml
